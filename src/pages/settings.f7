<template>
  <div class="page" data-name="settings">
    <div class="navbar">
      <div class="navbar-bg"></div>
      <div class="navbar-inner sliding">
        <div class="left">
          <a href="#" class="link back" @click="${handleBack}">
            <i class="icon icon-back"></i>
          </a>
        </div>
        <div class="title">Ajustes</div>
      </div>
    </div>
    <div class="page-content">
      <div class="list list-strong-ios list-dividers-ios list-outline-ios">
        <ul>

          <li>
            <div class="item-content item-input">
              <div class="item-inner">
                <div class="item-title item-label">Teléfono</div>
                <div class="item-input-wrap">
                  <input type="tel" placeholder="Teléfono" />
                </div>
              </div>
            </div>
          </li>
          <li>
            <div class="item-content item-input">
              <div class="item-inner">
                <div class="item-title item-label">Banco</div>
                <div class="item-input-wrap">
                  <input id="bank" type="text" placeholder="Banco" />
                </div>
              </div>
            </div>
          </li>

          <li>
            <div class="item-content">
              <div class="item-inner">
                <div class="item-title">Forzar Idioma</div>
                <div class="item-after">
                  <label class="toggle toggle-init">
                    <input type="checkbox" />
                    <span class="toggle-icon"></span>
                  </label>
                </div>
              </div>
            </div>

            <div class="item-content item-input">
              <div class="item-inner">
                <div class="item-input-wrap">
                  <select disabled>
                    <option>Inglés</option>
                    <option>Español</option>
                  </select>
                </div>
              </div>
            </div>
          </li>



        </ul>
      </div>


    </div>
  </div>
</template>
<script>
  import { db, getOption, saveOption, Keys } from 'db';
  export default async (props, { $f7, $on }) => {

    $on('pageInit', async () => {
      const banks = await db.banks.toArray();
      console.log(banks);

      let values = [];
      let displayValues = [];

      banks.forEach(bank => {
        values.push(bank.id);
        displayValues.push(bank.name);
      });

      let bankId = await getOption(Keys.SELECTED_BANK);

      const bankPicker = $f7.picker.create({
        inputEl: '#bank',
        cols: [
          {
            values,
            displayValues
          }
        ],
        formatValue: (values, displayValues) => {
          if (bankId) {
            const selectedBank = banks.filter(bank => bank.id === +bankId)[0];
            return selectedBank.name;
          }
        },
        value: [bankId],
        on: {
          change: (picker, values, displayValues) => {
            // const selectedId = bankPicker.value[0];
            // const selectedName = bankPicker.displayValue[0];
            const selectedId = values[0];
            bankId = selectedId;
            const selectedName = displayValues[0];
            console.log('bank id:', selectedId);
            saveOption(Keys.SELECTED_BANK, +selectedId);
          }
        }
      });
    })




    const handleBack = async () => {
      const bankId = await getOption(Keys.SELECTED_BANK)
      if (!bankId) {
        $f7.dialog.confirm(
          'No ha seleccionado su banco. Salir? ',
          () => {// ok
            const tabLink = document.querySelectorAll('.tab-link')[0]
            $f7.tab.show(`#view-home`, tabLink, true);
          },
          () => { // cancel
            $f7.views.current.router.navigate('/settings/')
          }
        );
      } else {
        $f7.views.current.router.back()
      }
    };





    return $render;
  };
</script>