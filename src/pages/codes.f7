<template>
  <div class="page" data-name="codes">
    <div class="navbar">
      <div class="navbar-bg"></div>
      <div class="navbar-inner sliding">
        <div class="title">Mis QRs</div>
      </div>
    </div>

    ${products.value.length > 0 ? $h`

    <div class="fab fab-right-bottom">
      <a @click="${onClickAdd}">
        <i class="icon f7-icons if-not-md">plus</i>
        <i class="icon f7-icons if-not-md">xmark</i>

        <i class="icon material-icons md-only">add</i>
        <i class="icon material-icons md-only">close</i>
      </a>

      <!-- buttons for later -->
      <!-- 
      <div class="fab-buttons fab-buttons-top">
        <a href="" class="fab-label-button">
          <span>1</span>
          <span class="fab-label">Editar</span>
        </a>
        <a href="" class="fab-label-button">
          <span>2</span>
          <span class="fab-label">Imprimir</span>
        </a>
        <a href="" class="fab-label-button">
          <span>3</span>
          <span class="fab-label">Borrar</span>
        </a>
      </div> 
      -->
    </div>
    `: ''}

    <div class="page-content">
      <div class="list media-list  list-outline-ios list-strong-ios list-dividers-ios">

        ${products.value.length === 0 ? $h`
        <div class="empty-state block text-align-center margin-top">
          <i class="icon f7-icons size-50 color-gray">archivebox</i>
          <p class="color-gray">No hay productos</p>
          <p>
            Agregue aquí un código QR por cada producto que quiera vender, por ejemplo:
            Palomitas a mil colones.
          </p>

          <div class="block">
            <button class="button button-fill popup-open" @click="${onClickAdd}">
              Agregar Nuevo
            </button>
          </div>

        </div>
        `: $h`
        <div class="block">
          <p>Seleccione un item de la lista para ver el código QR.</p>
        </div>
        `}


        <ul>
          ${products.value.map((product) => $h`
          <li>
            <a class="item-link item-content" href="/codes/${product.id}/">
              <div class="item-media">
                <i class="icon f7-icons if-not-md">qrcode</i>
                <i class="icon material-icons if-md">qr_code</i>
              </div>
              <div class="item-inner">
                <div class="item-title-row">
                  <div class="item-title">${product.name}</div>
                  <div class="item-after">₡${product.price}</div>
                </div>
                <div class="item-subtitle">${product.phone}</div>
                <div class="item-text">${product.detail}</div>

              </div>
            </a>
          </li>
          `)}
        </ul>
      </div>

    </div>

  </div>


</template>
<script>
  export default (props, { $f7, $store, $on, $el }) => {

    const products = $store.getters.products;

    const onClickAdd = () => {
      $f7.emit('openQRDataPopup', null);
    }

    return $render;
  }

</script>