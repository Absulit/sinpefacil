<template>
  <div class="page" data-name="history">
    <!-- Top Navbar -->
    <div class="navbar">
      <div class="navbar-bg"></div>
      <div class="navbar-inner">
        <!-- <div class="left">
          <a href="#" class="link icon-only panel-open" data-panel="left">
            <i class="icon f7-icons if-not-md">menu</i>
            <i class="icon material-icons if-md">menu</i>
          </a>
        </div> -->
        <div class="title sliding display-flex align-items-center">
          <div class="title">${i18next.t('history')}</div>
        </div>
        <div class="right">
          <a href="#" class="link icon-only panel-open" data-panel="right">
            <i class="icon f7-icons if-not-md">menu</i>
            <i class="icon material-icons if-md">menu</i>
          </a>

        </div>
        <!-- <div class="title-large">
          <div class="title-large-text">SINPE Fácil</div>
        </div> -->
      </div>
    </div>

    ${history.value.length > 0 ? $h`

    <!-- <div class="fab fab-right-bottom">
      <a>
        <i class="icon f7-icons if-not-md">plus</i>
        <i class="icon f7-icons if-not-md">xmark</i>

        <i class="icon material-icons md-only">add</i>
        <i class="icon material-icons md-only">close</i>
      </a>
    </div> -->
    `: ''}

    <div class="page-content">
      <div class="list media-list  list-outline-ios list-strong-ios list-dividers-ios">

        ${history.value.length === 0 ? $h`
        <div class="empty-state block text-align-center margin-top">

          <i class="icon f7-icons if-not-md">archivebox</i>
          <i class="icon material-icons if-md">history_toggle_off</i>


          <p class="color-gray">${i18next.t('history:noHistory')}</p>
          <p>${i18next.t('history:noHistoryExplainer')}</p>
        </div>
        `: $h`
        <div class="block">
          <p>${i18next.t('history:explainer')}</p>
        </div>
        `}

        <ul>
          ${history.value.map((historyItem) => $h`

          <li>
            <a class="item-link item-content" href="/history/${historyItem.id}/">
              <div class="item-media">
                <i class="icon f7-icons if-not-md">checklist</i>
                <i class="icon material-icons if-md">fact_check</i>
              </div>
              <div class="item-inner">
                <div class="item-title-row">
                  <div class="item-title">${historyItem.name}</div>
                  <div class="item-after">₡${historyItem.price}</div>
                </div>
                <div class="item-subtitle">${historyItem.phone}</div>
                <div class="item-text">${formatDate(historyItem.createdAt)}</div>

              </div>
            </a>
          </li>

          `)}
        </ul>
      </div>

    </div>

  </div>


</template>
<script>
  import { formatDate } from 'i18n';
  import i18next from 'i18next';
  export default (props, { $f7, $store, $on, $el }) => {

    const history = $store.getters.history;
    return $render;
  }

</script>