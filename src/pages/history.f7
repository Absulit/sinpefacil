<template>
  <div class="page" data-name="history">
    <div class="navbar">
      <div class="navbar-bg"></div>
      <div class="navbar-inner sliding">
        <div class="title">Historial</div>
      </div>
    </div>

    ${history.value.length > 0 ? $h`

    <div class="fab fab-right-bottom">
      <a @click="${onClickAdd}">
        <i class="icon f7-icons if-not-md">plus</i>
        <i class="icon f7-icons if-not-md">xmark</i>

        <i class="icon material-icons md-only">add</i>
        <i class="icon material-icons md-only">close</i>
      </a>
    </div>
    `: ''}

    <div class="page-content">
      <div class="list list-strong list-dividers-ios list-outline-ios inset-md">

        ${history.value.length === 0 ? $h`
        <div class="empty-state block text-align-center margin-top">

          <i class="icon f7-icons if-not-md">archivebox</i>
          <i class="icon material-icons if-md">history_toggle_off</i>


          <p class="color-gray">No hay historial</p>
          <p>
            Lea algún código QR o acepte un link de pago y le aparecerá luego aquí.
          </p>
        </div>
        `: ''}


        ${history.value.map((product) => $h`
        <div class="block">
          <p>Seleccione un item de la lista para ver el código QR.</p>
        </div>
        <ul>
          <li>
            <a class="item-content item-link" href="/product/${product.id}/">
              <div class="item-media">
                <i class="icon f7-icons if-not-md">qrcode</i>
                <i class="icon material-icons if-md">qr_code</i>
              </div>
              <div class="item-inner">
                <div class="item-title">${product.name} - ₡${product.price} </div>
              </div>
            </a>
          </li>
        </ul>
        `)}
      </div>






    </div>





  </div>


</template>
<script>
  export default (props, { $f7, $store, $on, $el }) => {

    const history = $store.getters.history;

    return $render;
  }

</script>
