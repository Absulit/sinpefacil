<template>
  <div class="page" data-name="home">
    <!-- Top Navbar -->
    <div class="navbar navbar-large">
      <div class="navbar-bg"></div>
      <div class="navbar-inner">
        <div class="left">
          <a href="#" class="link icon-only panel-open" data-panel="left">
            <i class="icon f7-icons if-not-md">menu</i>
            <i class="icon material-icons if-md">menu</i>
          </a>
        </div>
        <div class="title sliding">sinpefacil</div>
        <div class="right">
          <a href="#" class="link icon-only panel-open" data-panel="right">
            <i class="icon f7-icons if-not-md">gear_alt_fill</i>
            <i class="icon material-icons if-md">settings</i>
          </a>
        </div>
        <div class="title-large">
          <div class="title-large-text">sinpefacil</div>
        </div>
      </div>
    </div>

    <!-- Scrollable page content-->
    <div class="page-content">
      <div class="block">
        <div class="block-title">Acerca de:</div>
        <p>
          Esta aplicación permite generar Códigos QR (?) para permitir a otras personas pagarte con SINPE MÓVIL. (?)
        </p>

        <p>
          También te permite leer códigos QR de otras personas y enviar un SMS para pagarles via SINPE MÓVIL.
        </p>

      </div>
      <div class="block">
        <div class="block-title">Seleccionar qué pantalla cargar al inicio:</div>
        <p>Prefiere que abra directamente a la cámara o prefiere cargar su lista de códigos QR?</p>
        <p>Si ud es vendedor, preferirá Mis QRs, si es comprador preferirá Leer y Pagar.</p>

        <div class="segmented segmented-strong">
          <button class="button ${activeStrongButton===0 ? 'button-active' : '' }"
            @click=${()=>setStrongButton(0)}>${i18next.t('home')}</button>
          <button class="button ${activeStrongButton===1 ? 'button-active' : '' }"
            @click=${()=>setStrongButton(1)}>${i18next.t('codes')}</button>
          <button class="button ${activeStrongButton===2 ? 'button-active' : '' }"
            @click=${()=>setStrongButton(2)}>${i18next.t('read')}</button>
          <span class="segmented-highlight"></span>
        </div>
      </div>

      <div class="block">
        Hecho en Costa Rica 🇨🇷 por <a href="https://mastodon.social/@absulit">@absulit</a>
      </div>

    </div>
  </div>
</template>
<script>
  import i18next from 'i18next';
  import { getOption } from 'db';
  export default async (props, { $f7, $update }) => {


    const pageIndexToLoad = await getOption('pageIndexToLoad');
    console.log(pageIndexToLoad);


    let activeStrongButton = 0;
    const setStrongButton = index => {
      console.log(index);
      // TODO: save data to indexDB
      activeStrongButton = index;
      $update();
    }
    return $render;
  }
</script>